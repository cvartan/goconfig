# go-config

`go-config` — лёгкий Go-пакет для доступа и управления конфигурацией приложения.

Значения конфигурации загружаются из следующих источников (в порядке приоритета):

1. Файлы конфигурации (JSON, YAML, TOML)
2. Переменные окружения ОС (имеют приоритет)
3. Файл `.env` (автоматически загружается из рабочего каталога приложения)

Поддерживается автоматическое связывание значений конфигурации с экспортируемыми полями структур.

---

## Возможности

- Конфигурационные файлы JSON, YAML, TOML
- Поддержка переменных окружения (с значениями по умолчанию)
- Автоматическая загрузка файла `.env` из рабочего каталога
- Связывание значений конфигурации с полями структур через struct tags
- Автоматическое определение типов (`string`, `bool`, `int64`,`float64`)
- Минимальный и явный API в духе Go

---

## Установка

```bash
go get github.com/cvartan/goconfig
```

---

## Требования к конфигурационным файлам

### Общие

- Поддерживаемые типы значений: `string`, `bool`, числовой тип (при сохранении приводится к int64 или float64)
- Значения неподдерживаемых типов игнорируются
- В качестве значения можно указать ссылку на переменную окружения:

```
${ENV_VARIABLE:DEFAULT_VALUE}
```

Тип значения определяется либо по переменной окружения, либо по значению по умолчанию. 
При этом значение по умолчанию может отсутствовать (в этом случае формат `${ENV_VARIABLE}`). 

Если значение параметра - строка, то можно использовать подстановку значения внутри значения.
```
{
    "url":"{$SRV_PROTOCOL:https}://{$SRV_NAME}:{$SRV_PORT:443}/api"
}
```

### Поддержка файла `.env`

Библиотека автоматически загружает переменные окружения из файла `.env` в рабочем каталоге приложения (если он существует).

Формат:
```
# Строки комментариев начинаются с #
DB_HOST=localhost
DB_PORT=5432
DEBUG=true
```

- Переменные окружения ОС имеют приоритет над значениями из файла `.env`
- Файл `.env` является опциональным и игнорируется без ошибок, если не найден
- Поддерживается формат `KEY=VALUE` с опциональными кавычками

### JSON и YAML

- Корневым элементом должен быть объект
- Использование массива в качестве корневого элемента не поддерживается

---

## Использование

### Парсеры конфигурации

Доступны три парсера:

- `JsonConfigurationParser` (для `Format = "json"`)
- `YamlConfigurationParser` (для `Format = "yaml"`)
- `TomlConfigurationParser` (для `Format = "toml"`)

Но можно создать собственный парсер (интерфейс `Parser`) и подключить его через метод `RegisterParser(format string, parser Parser)`.

```go
type Parser interface {
	Parse(data []byte) (map[string]any, error)
}
```

---

### Расположение конфигурационного файла

Файл конфигурации может быть задан одним из способов:

1. **По умолчанию** — `config.json` в рабочем каталоге приложения
2. **Статически** — через опции, при создании конфигурации
3. **Динамически** — через флаги приложения:

- `-config.path=/opt/app/config`
- `-config.filename=config.yml`
- `-config.format=yaml`
- `-config.source=/opt/app/config/config.yml` (при указании этого параметра, параметры config.path и config.filename не используются)

---

### Опции конфигурации

Структура `Options` используется для настройки менеджера конфигурации:

```go
type Options struct {
    Path     string // Путь к каталогу с файлом конфигурациии (без завершающего разделителя)
    Filename string // Имя файла конфигурации (по умолчанию: config.json)
    Format string // Формат представления данных в конфигурации (по умолчанию доступны два формата: json, yaml или toml)
    Source string // Путь к конфигурации (при добавлении собственных ридеров может указываться путь не только к файлу) 
}
```

---

## Привязка структур к конфигурации (метод Bind)

Значения параметров конфигурации могут быть связаны с экспортируемыми полями структур с помощью struct tag `config`.

Правила:

- Поля должны быть экспортируемыми
- Тип поля должен соответствовать типу значения конфигурации
- Если параметр отсутствует в файле, используется переменная окружения (если она задана)


Пример:

```go
package main

import (
    "github.com/cvartan/goconfig"
)

type AppConfig struct {
    Name  string `config:"app.name"`
    Debug bool   `config:"app.debug"`
}

func main() {
    var cfg AppConfig

    config := goconfig.NewConfiguration(nil) // Используем опции по умолчанию

    config.Bind(&cfg) // Заполняем поля структуры
    config.Apply() // Читаем конфигурацию из файла и применяем значения параметров на атрибуты связанных структур
}
```

---

## API

| Метод | Описание |
|------|----------|
| `NewConfiguration(format string, options ConfigurationOptions) *Configuration` | Создание менеджера конфигурации |
| `Configuration.Bind(object any)` | Привязка структуры к конфигурации (в структуре должны быть атрибуты, отмеченные тэгом config) |
| `Configuration.Apply()` | Загрузка и применение конфигурации |
| `Configuration.Add(key string, valueType ValueDataType)`| Добавление параметра без установки значения |
| `Configuration.Set(key string, value any)` | Установка значения параметра (или добавление при его отсутствии) |
| `Configuration.Get(key string) *Parameter` | Получение параметра конфигурации |
| `Configuration.GetAll() []*Parameter` | Получение всех параметров |
| `Configuration.Lookup(filter string) []*Parameter` | Получение массива параметров по префиксу |
| `Configuration.GetArrayValues(key string) []any` | Получение значений массива параметров |
| `Configuration.GetIntArray(key string) []int64` | Получение значений для массива параметров с целочисленными значениями |
| `Configuration.GetFloatArray(key string) []float64` | Получение значений для массива параметров со значениями в виде числа с плавающей точкой |
| `Configuration.GetStringArray(key string) []string` | Получение массива строковых значений для массива параметров |
| `Parameter.Name() string` | Получение имени параметра |
| `Parameter.Value() any` | Получение raw-значения параметра |
| `Parameter.Type reflect.Kind` | Получение обощенного типа значения параметра | 
| `Parameter.Int() int64` | Получение значения параметра с типом int |
| `Parameter.Float() float64` | Получение значения параметра с типом float64 |
| `Parameter.Bool() bool` | Получение значения параметра с типом bool |
| `Parameter.String() string` | Получение значения параметра с типом string |


---

## Ограничения

- Поля структур, являющиеся срезами или массивами, не поддерживаются

---

## Лицензия

MIT

